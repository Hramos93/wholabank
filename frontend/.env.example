# Este archivo sirve como ejemplo para el desarrollo local.
# Copia este archivo a .env.local y ajústalo según tu configuración.
# El archivo .env.local NO debe ser subido a Git.

# --- Opcional ---
# En la nueva configuración, no necesitas esta variable si usas el proxy de Vite.
# Sin embargo, la dejamos aquí como documentación por si decides volver a usarla.
# VITE_API_URL=http://127.0.0.1:8000/api/
